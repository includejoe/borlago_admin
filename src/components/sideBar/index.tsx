import { SideBarContainer } from "./styles";

const SideBar = () => {
  return (
    <SideBarContainer isShowing={true}>
      <h1>Side Bar</h1>
    </SideBarContainer>
  );
};

export default SideBar;
